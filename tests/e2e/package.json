{
  "name": "@semiont/tests-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end smoke tests. Runs against a locally-running backend and frontend; see README.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "show-report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@semiont/core": "file:../../packages/core",
    "@semiont/sdk": "file:../../packages/sdk",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.2"
  }
}
